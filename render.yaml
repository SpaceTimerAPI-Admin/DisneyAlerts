services:
  - type: worker
    name: disney-dining-bot
    env: python
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: DISNEY_USERNAME
        sync: false
      - key: DISNEY_PASSWORD
        sync: false
